# Security policy

This project parses local JSON and JSONL files and performs no code execution from benchmark submissions. Report malformed-input crashes, path traversal, unsafe network behavior, or source-integrity issues privately through GitHub's security advisory interface. Do not include live credentials or private agent traces in reports.
